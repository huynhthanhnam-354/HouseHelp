import React from "react";
import AuthPageView from '../views/AuthPageView';

export default function AuthPageController(props) {
  // Nếu có logic xử lý, fetch data, state... sẽ đặt ở đây
  return <AuthPageView {...props} />;
} 